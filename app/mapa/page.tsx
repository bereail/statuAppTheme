import { MapPin } from "lucide-react";

export default function MapaPage() {
  return (
    <main className="flex flex-col items-center justify-center py-20 text-center space-y-4">
      <MapPin size={64} className="text-primary" />
      <h1 className="text-3xl font-bold">PRÓXIMAMENTE MAPA INTERACTIVO</h1>

    </main>
  );
}
